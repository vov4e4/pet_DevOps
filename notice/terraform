INSTALL TERRAFORM IN RASPBERRY PI
https://snapcraft.io/install/terraform/raspbian

  sudo apt update
  sudo apt install snapd
  sudo reboot
  sudo snap install core
  sudo snap install terraform --classic
  terraform version
REFRESH VERSION


